# Customers-Management-Django-Using-React

# For the backend
- create and activate virtual env using:
  - python -m venv ./env_name
  - pip install -r requirements  
    
# For the Frontend
- npm install 
- npm start

Enjoy !